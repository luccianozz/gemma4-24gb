services:
  llama:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    container_name: gemma4-12b
    # Direct host port for default deploys. To run the bundled nginx proxy
    # (`--profile proxy`) at scale, remove `container_name` + `ports` and
    # use `expose: ["8000"]` so `--scale llama=N` works.
    runtime: nvidia
    ipc: host
    ports:
      - "8000:8000"
    volumes:
      - llama_cache:/root/.cache/llama.cpp
    environment:
      - HF_TOKEN=${HF_TOKEN}
      - LLAMA_API_KEY=${LLAMA_API_KEY}
    command:
      - -hf
      - bartowski/gemma-4-12B-it-GGUF:Q4_K_M
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --n-gpu-layers
      - "-1"
      - --flash-attn
      - "on"
      - --ctx-size
      - "8192"
      - --parallel
      - "2"
      - --batch-size
      - "2048"
      - --ubatch-size
      - "2048"
      - --cache-type-k
      - f16
      - --cache-type-v
      - f16
      - -hfd
      - unsloth/gemma-4-E2B-it-GGUF:Q4_K_M
      - --spec-type
      - draft-simple
      - --spec-draft-n-max
      - "24"
      - --spec-draft-n-min
      - "4"
      - --spec-draft-p-min
      - "0.4"
      - --gpu-layers-draft
      - "99"
      - --api-key
      - ${LLAMA_API_KEY}
      - --metrics
      - --reasoning-format
      - deepseek
      - --reasoning-budget
      - "256"
      - --alias
      - gemma-4-12b
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -fsS -H \"Authorization: Bearer $$LLAMA_API_KEY\" http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 600s
    restart: unless-stopped

  proxy:
    image: nginx:1.27-alpine
    container_name: gemma4-proxy
    profiles: ["proxy"]
    depends_on:
      - llama
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/nginx-health"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  bench:
    image: python:3.12-slim
    profiles: ["bench"]
    container_name: gemma4-12b-bench
    depends_on:
      llama:
        condition: service_healthy
    environment:
      - LLAMA_API_KEY=${LLAMA_API_KEY}
      - LLAMA_URL=http://llama:8000
      - MODEL=gemma-4-12b
      - CONCURRENCY_LEVELS=${CONCURRENCY_LEVELS:-1,4,8,16,32}
      - MAX_TOKENS=${MAX_TOKENS:-300}
      - PROMPT=${PROMPT:-Count from 1 to 100, one number per line.}
    volumes:
      - ./bench:/bench:ro
    command: ["sh", "-c", "pip install --quiet --no-cache-dir httpx==0.27.* && python /bench/run.py"]

volumes:
  llama_cache:
